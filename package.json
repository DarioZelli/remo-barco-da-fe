{
  "name": "remo-redemundial",
  "version": "1.0.0",
  "description": "Site institucional da REMO - Rede Mundial de Orações",
  "private": true,
  "scripts": {
    "dev": "npx netlify dev",
    "build": "echo 'Sem build de bundler: deploy direto de HTML + Netlify Functions'",
    "start": "npm run dev",
    "check:structure": "test -f index.html && test -d netlify/functions && echo 'OK: estrutura base encontrada'"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
